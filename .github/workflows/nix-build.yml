on:
  workflow_call:

jobs:
  build:
    uses: hyprwm/actions/.github/workflows/nix.yml@main
    secrets: inherit
    with:
      command: |
        nix flake update
        nix flake check -L --keep-going
